import 'dart:ui';
import 'package:flutter/material.dart';

class AppColors {
  // Primary palette (branding)
  static const Color primary = Color(0xff175fa4);
  //static const Color primaryDark = Color(0xFF004999);
  //static const Color primaryLight = Color(0xFF3385FF);

  // Accent
  //static const Color accent = Color(0xFFFF9800);

  // Background
  static const Color background = Color(0xffe5e7eb);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color chipBackground = Color(0xffe5e7eb);

  // Text colors
  static const Color textPrimary = Color(0xFF1E1E1E);
  static const Color textSecondary = Color(0xFF666666);

  // Borders / dividers
  static const Color border = Color(0xFFE3E7EB);

  // Status
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFF44336);

  // Disable state
  static const Color disabled = Color(0xFFBDBDBD);

  // Shadows
  static const Color shadow = Color(0x33000000); // 20% opacity
}